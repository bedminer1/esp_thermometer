[package]
name = "host_collector"
version = "0.1.0"
edition = "2021"

[dependencies]
serialport = "4.6.1"
postcard = { version = "1.0", features = ["use-std"] }
serde = { version = "1.0", features = ["derive"] }
# We will define the struct here for simplicity in this exercise
